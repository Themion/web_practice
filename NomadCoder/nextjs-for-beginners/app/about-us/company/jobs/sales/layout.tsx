import { PropsWithChildren } from "react";

const SalesLayout = ({ children }: PropsWithChildren) => {
  return (
    <div>
      {children}
      $$$$$
    </div>
  );
};

export default SalesLayout;
